class Solution:
    def search(self, nums: List[int], target: int) -> int:
        count=-1
        t=0
        for i in nums:
            if i==target:
                t+=1
                return count+1
            count+=1;
        if t==0:
            return -1;
        
